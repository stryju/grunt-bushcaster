grunt-requirejs-cachebuster
===========================
the name says it all, doesn't it?

sample:

```js
'requirejs-cachebuster' : {
  files : [
    'test/dist/**/*.js'
  ],

  options : {
    dir : 'test/dist'
  }
}
```
